# Game State

Runtime game state schema and definitions.

## Future Contents

- `schema.json` -- Game state schema definition (what properties exist, their types, valid ranges)
- Used by the Director Dashboard to render real-time state
- Used by the AI Director agent to understand current game state
