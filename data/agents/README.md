# Agent Data

Runtime configuration and registry for game agents.

## Future Contents

- `registry.json` -- Which agents are active and their entity bindings
- `npc/` -- Per-NPC configuration overrides
- `camera/` -- Camera agent configuration
- `lighting/` -- Lighting agent configuration
